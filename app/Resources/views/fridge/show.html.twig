{% extends 'base.html.twig' %}

{% block body %}
    <h1>Fridge</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fridge.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fridge.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('fridge_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('fridge_edit', { 'id': fridge.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
